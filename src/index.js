import React, { PureComponent } from 'react';

import './styles.scss';

export default class VerificationInput extends PureComponent {
  render () {
    return (
      <div className="purple semi-bold">Verification input :)</div>
    );
  }
}